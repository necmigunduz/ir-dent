import React from 'react';
import { Typography } from '@material-ui/core';

const Home =  () => {
  return (
      <Typography variant="h4">Welcome to Homepage of BookList Application!</Typography>
  );
};

export default Home;